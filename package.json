{
  "name": "facebook-video-downloader",
  "version": "0.2.11",
  "private": true,
  "description": "Cloudflare Worker serving a single plain page that saves the video or photo from a Facebook post as a file.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node test/extract.test.mjs",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
