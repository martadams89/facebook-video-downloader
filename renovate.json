{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommits",
    ":dependencyDashboard"
  ],
  "labels": [
    "dependencies"
  ],
  "rangeStrategy": "bump",
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true
  },
  "packageRules": [
    {
      "description": "Auto-merge everything - major/minor/patch/pin/digest - once required CI is green (extractor tests + Wrangler bundle). CI is the gate, not semver.",
      "matchUpdateTypes": [
        "major",
        "minor",
        "patch",
        "pin",
        "digest"
      ],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "Wrangler majors can change wrangler.jsonc semantics and the Workers runtime API. Still auto-merges (the bundle step in CI catches a broken config), but flagged for a look.",
      "matchPackageNames": [
        "wrangler"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": true,
      "addLabels": [
        "needs-review"
      ]
    }
  ],
  "prConcurrentLimit": 5,
  "prHourlyLimit": 3,
  "description": "Self-hosted via .github/workflows/renovate.yml (daily cron + manual dispatch); cadence lives there, not here."
}
